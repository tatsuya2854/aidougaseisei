# aidougaseisei — 作業前に読むこと

## 1. 着手前に必ず Obsidian ノートを読む

`obsidian/ChenMe/` はこの案件の知見をためた Obsidian vault。
**コードを触る前に、最低でも次の3つを読む。**

1. `obsidian/ChenMe/00 プロジェクト索引.md`
2. `obsidian/ChenMe/10 絶対制約.md` — 破ると契約違反・やり直しになるルール
3. `obsidian/ChenMe/70 次回の初手.md` — 最初の30分でやること

読まずに始めると、そこに書いてある失敗をもう一度やることになる。
実際に3回やり直しになっている。

## 2. 「記録して」と言われたら Obsidian に書く

施主が「記録して」「記録しておいて」と言ったら、
**確認せずに** `obsidian/` 配下へ Markdown で追記・新規作成する。
毎回聞き返さない。

書き方の約束：
- YAML frontmatter（`title` / `tags` / `updated`）を付ける
- `[[wikilink]]` で相互に繋ぐ
- 失敗は「何を間違えたか」ではなく **「なぜそう判断したか」** を書く
- 数値は実測値を書く（目分量の記憶を残さない）

このセッションには Obsidian コネクタが無いので、リポジトリ内の
`obsidian/` に書いて git で同期する。施主がそれを vault に置く。

## 3. この案件の一行

支給素材の見た目を変えずに動かそうとすると必ず行き詰まる。
**「どこまで変えていいか」を最初に確定させる。**
